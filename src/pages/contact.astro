---
import Layout from '../layouts/Layout.astro';
import { getEntry } from 'astro:content';

const contactEntry = await getEntry('contact', 'index');
const contact = contactEntry?.data;
---

<Layout title="Contact">
  <section class="hero">
    <div class="hero-grid">
      <div class="hero-text">
        <div class="mission-id">COMMS_LINK_V4</div>
        <h1>GET IN <span class="hl-wrap"><div class="hl"></div><span class="hl-text">TOUCH</span></span></h1>
        
        <div class="specs" style="margin-top: 3rem;">
          <div class="spec-row">
            <span class="spec-label">ENCRYPTED_EMAIL</span>
            <span class="spec-val"><a href={`mailto:${contact?.email}`}>{contact?.email}</a></span>
          </div>
          
          {contact?.socials.map((s) => (
            <div class="spec-row">
              <span class="spec-label">{s.platform.toUpperCase()}</span>
              <span class="spec-val"><a href={s.url} target="_blank">{s.handle}</a></span>
            </div>
          ))}
        </div>
      </div>
    </div>
  </section>
</Layout>
